//! Static Explorer data contract.
//!
//! Browser rendering belongs to the compiled `explorer/` SPA. Rust builds the
//! normalized Project Store data that `serve` exposes as a runtime asset.

pub mod store;
