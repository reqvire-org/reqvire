# Table

Use `TableViewport`, `Table`, `TableHeader`, `TableBody`, `TableRow`,
`TableHead`, `TableCell`, and `TableSortButton` for dense file/model/result
tables. Keep tables flat, high contrast, and token-driven. Selection is a row
state (`TableRow selected`) with a soft slate background, not an inner bordered
rectangle.
