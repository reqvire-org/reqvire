Tiny count / status pill — sits beside tree rows, folders, file names and tabs.

```jsx
<Badge>8</Badge>
<Badge variant="accent">3</Badge>
<Badge variant="dot" />
```

Variants: `default` (neutral count), `accent` (teal), `solid` (dark), `dot` (7px status dot, no text).
