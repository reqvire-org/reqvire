Search field with a leading magnifier and optional keyboard hint. Inherits all native `<input>` props.

```jsx
<SearchInput placeholder="Search ontology graph…" />
<SearchInput size="lg" placeholder="Search elements, files, resources, ontology terms…" kbd="/" />
```
