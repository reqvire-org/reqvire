# Export Specification

## Overview

This document specifies the export functionality for data exchange.

## Format

Data is exported in JSON format following the standard schema.

## Requirements

- All exported data must be UTF-8 encoded
- File names must follow the pattern: export_YYYYMMDD.json
