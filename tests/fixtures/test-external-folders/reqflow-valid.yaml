# Test configuration with external folders

general:
  verbose: true

paths:
  specifications_folder: "specifications"
  output_folder: "output"
  external_folders:
    - "external-project"

style:
  theme: "default"
  max_width: 1200
  diagram_direction: "TD"
